{
  "name": "nog-jwt-2",
  "version": "0.0.0",
  "description": "Nog JWT auth",
  "main": "index-server.js",
  "scripts": {
    "lint": "\"$(git rev-parse --show-toplevel)\"/node_modules/.bin/eslint .",
    "lint-fix": "meteor npm run lint -- --fix",
    "test": "npm run test-headless",
    "test-watch": "echo >&2 'nog-jwt-2 has no unit tests.' && exit 0 && cd \"$(git rev-parse --show-toplevel)\"/web/apps/packages-testapp && npm run test-packages-watch-nog-jwt-2",
    "test-headless": "echo >&2 'nog-jwt-2 has no unit tests.' && exit 0 && cd \"$(git rev-parse --show-toplevel)\"/web/apps/packages-testapp && npm run test-packages-headless-nog-jwt-2"
  },
  "keywords": [
    "Nog"
  ],
  "author": "Steffen Prohaska <prohaska@zib.de>",
  "license": "MIT",
  "//": "See toplevel package.json for ESLint dependencies",
  "devDependencies": { }
}
